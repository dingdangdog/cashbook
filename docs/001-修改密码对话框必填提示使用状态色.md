# 修改密码对话框必填提示使用状态色

## 日期

2026-03-21

## 变更说明

`ChangePasswordDialog.vue` 中字段校验提示由 `text-red-600 dark:text-red-400` 改为与全站一致的 `text-state-danger dark:text-state-danger`（与 `tailwind.config.js` 中 `theme.extend.colors.state.danger` 对齐），确保必填/不一致提示在亮暗主题下均显示为明确的红色语义色。

## 涉及文件

- `app/components/dialog/ChangePasswordDialog.vue`
